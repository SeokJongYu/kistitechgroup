<%= form_for(@attendance_list) do |f| %>
  <% if @attendance_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attendance_list.errors.count, "error") %> prohibited this attendance_list from being saved:</h2>

      <ul>
      <% @attendance_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :talk_id %><br />
    <%= f.number_field :talk_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
